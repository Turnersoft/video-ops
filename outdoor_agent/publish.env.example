# Outdoor publish credentials (Mac agent)
# Never commit real keys.

# --- Zernio (English) ---
# https://zernio.com — Social Media API
# 1) Sign up / log in
# 2) Dashboard → API keys → ZERNIO_API_KEY
# 3) Connect channels (YouTube, X, LinkedIn, IG, TikTok, …)
# 4) #/platforms → Sync accounts → ZERNIO_PUBLISH_MODE=live
export ZERNIO_API_KEY=
export ZERNIO_ACCOUNTS_JSON='{}'
export ZERNIO_PUBLISH_MODE=stub
# export ZERNIO_PUBLISH_MODE=live
# export ZERNIO_API_BASE=https://zernio.com/api/v1

# --- SAU (China) ---
export SAU_BIN=/Users/johndoe/.local/bin/sau
export SAU_REPO=/Users/johndoe/Documents/company/social-auto-upload
export SAU_ACCOUNT=default
export SAU_PUBLISH_MODE=stub
# export SAU_PUBLISH_MODE=live
